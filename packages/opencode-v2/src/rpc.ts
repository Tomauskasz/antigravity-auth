// OpenCode 2's cross-platform package resolver probes an `/rpc` export even
// when `oc-plugin` enables only the server entry. This module intentionally
// exposes no RPC registrations.
export const id = 'cortexkit.antigravity-auth.rpc-placeholder'

export default { id }
